import React from 'react';
import { Line } from '../Layout.styles';
import { HeaderContainer, BannerContainer } from './Header.styles';
import { useHistory } from 'react-router-dom';

export const Header = () => {
  return (
    <HeaderContainer>
    </HeaderContainer>
  );
};
